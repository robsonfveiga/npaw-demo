
/**
 * Enum representing the available API endpoints for NPAW services.
 * Each value corresponds to a specific API endpoint path.
 * 
 * @enum {string}
 */
export enum ApiEndpoints {
  /**
   * Endpoint for retrieving metrics data
   */
  Metrics = '/data'
}